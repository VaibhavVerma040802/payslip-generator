{
  "name": "payslip-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd .. && cd backend && npm install",
    "build": "cd frontend && npm install && npm run build && cd .. && if exist dist rd /s /q dist && xcopy /E /I frontend\\dist dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "nodemailer": "^6.9.7",
    "pdfkit": "^0.14.0"
  }
}
